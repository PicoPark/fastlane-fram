osx_image: xcode8.1
language: objective-c
cache:
  - bundler
  - cocoapods
branches:
  only:
    - master
    - develop
notifications:
  email: false
before_install:
  - gem install bundler
  - bundle install
script:
  - bundle exec fastlane bonjour
